# MMB integration packages published to GCP Artifact Registry.
# One package name per line (must match src/integrations/<name>/).
# Core prefect is always published when release_python=true.
prefect-gcp
prefect-dbt
